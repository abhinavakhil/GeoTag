# Optional shared backend. Leave unset and GeoTag runs fully client-side with localStorage.
# 1. Create a project at https://supabase.com  2. Run supabase/schema.sql in the SQL editor  3. Copy these from Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
